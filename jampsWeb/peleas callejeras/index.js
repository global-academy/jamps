$(function(){
	var nuevocontenido = "",
		$maincontent =$("#contenido.menu"),
		$pagewrap  =$("#page-wrap"),
		tambase   = 0;
		
$pageWrap.height.($patgeWarp.height());
tamBase = $pagewrapWrap.height() - $maincontenido.height();

$(".menu").delegate("a", "click", function(){
		window.location.hash =$(this).attr("href");
		return false;
		});

		$(window).bind('hashchange', function()   {

			nuevo contenido= window.location.hash.substring(1);
				
				if(nuevocontenido.length > 0){
					$

}

});